# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1

# PDF parsing
pdfplumber==0.11.0

torch --index-url https://download.pytorch.org/whl/cpu
# Embeddings — multilingual (English + Arabic), runs locally, free
sentence-transformers==3.0.1
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.3.1+cpu
# Vector database
chromadb==0.5.3

# LLM HTTP calls (no heavy SDK needed)
httpx==0.27.0

# Language detection
langdetect==1.0.9

# Data validation (FastAPI uses this internally, but pinning for consistency)
pydantic==2.7.4

# Utilities
python-multipart==0.0.9   # needed for FastAPI file uploads
